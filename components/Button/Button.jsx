import {Button, OutlineButton } from "index"
import React from 'react'

export default Button = (props) => {
    return (
        <div>
            <Button>
                {props.Button}
            </Button>
        </div>
    )
}
